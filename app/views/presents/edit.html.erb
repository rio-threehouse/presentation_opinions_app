<div class="panel">
  <div class="panel-heading">
    <h4 class="text-center">報告内容の変更</h4>
    <%= link_to '←戻る', main_path(@present.main), class: 'back-btn' %>
  </div>
  <div class="panel-body">
    <%= form_for(@present) do |f| %>
      <div class="modal-body">
        <div class="form-group">
          <%= f.label :title, 'タイトル' %> <span class="text-muted small">（最大50文字）</span>
          <%= f.text_field :title, class: 'form-control', required: true %>
        </div>
        <div>
          登録者：<%= current_user.name %>
        </div>
      </div>

      <div class="modal-footer">
        <div class="text-rmuted" style="text-align: left">
          ※登録した報告内容の情報変更、削除は<%= current_user.name %>さんのみ行うことができます
        </div>

        <%= f.submit '変更する', class: 'btn nomal-btn' %>
      </div>
    <% end %>
  </div>
</div>
