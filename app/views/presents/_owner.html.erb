<%# オーナー雛形 %>
<% breadcrumb :present_owner, present %>
<div class="row"> 
  <div class="col-sm-8 col-sm-offset-2">
    <%= render 'presents/owner_check_top', present: present %>
    
    <%= render 'presents/commenttag_btn', present: present %>
    
    <div class="panel-body">
      <%= render 'comments/comments', comments: comments, present: present %>
      <div class="text-center">
        <%= paginate comments %>
      </div>
    </div>
  </div>
</div>
